# pnpm 11 runs `pnpm install` before each script (verify-deps-before-run) and
# that install fails on the sharp build-script gate. Dev doesn't need sharp's
# native build, so skip the pre-run check.
verify-deps-before-run=false
